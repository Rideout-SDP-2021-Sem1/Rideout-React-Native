const { userRoute } = require('./user')
const { locationRoute } = require("./location")
const { groupRoute } = require("./group")

module.exports = {
  userRoute,
  locationRoute,
  groupRoute
}